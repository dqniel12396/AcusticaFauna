# Manifests ML

Los manifests reales pueden contener rutas locales, nombres de audios y datos de investigacion. Por defecto no se versionan.

Usa esta carpeta para colocar manifests propios despues de clonar el proyecto. Los manifests limpios generados por la web se escriben en `manifests/clean/`.
